# Onboarding — data

Fase atual (local-only, ver `docs/adr/0006-mvp-local-only.md`): preferências,
equipamento e consentimentos gravados em tabelas Drift locais, sem
`profiles`/conta. Quando o backend voltar, migra para
`profiles`/`consents`/`user_preferences`/`user_equipment` no Supabase.

Referência: `08_ARCHITECTURE/DATA_MODEL.md`.
